/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    appDir: true,
  },
  // distDir: "build",
  // output: "export",
}
export default nextConfig
